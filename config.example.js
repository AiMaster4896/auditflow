// Copy this file to config.js and fill in your Supabase project's values.
// config.js is gitignored — never commit real keys.
//
// Both values are safe to expose in a browser (protected entirely by
// Row Level Security) — but keep them out of the public repo anyway so a
// fork or clone doesn't accidentally point at this firm's live project.
window.AUDITFLOW_CONFIG = {
  supabaseUrl: "YOUR_SUPABASE_URL",
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY"
};
